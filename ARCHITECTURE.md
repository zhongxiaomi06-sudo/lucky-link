# Lucky Link 架构

## V44 入口与模块

独立 Vite 应用，原生 ES Modules／DOM／Canvas2D，Three.js 0.185.0 用于 3D 展示。无服务端、账号、远程模型推理或外部运行 API。

默认 `home-entry.js` 导入 `tabletop.js`，保留完整手机链 Hook 与既有底座，再通过 `collection-ui.js` 打开托盘和揭晓。显式 `?studio=1` 保留工作台，`?view=3d` 保留历史 3D。`ice-blue-drop.js` 是保留实验代码，不是当前默认路由；不能把其四身份规则描述为生产主线。

| 模块 | 当前职责 |
| --- | --- |
| `gravity-game.js` | 320×500 物理世界、60 件出生、5 身份袋、碰撞接触组、积分／连锁／倒计时 |
| `material-physics.js` | 玻璃／珍珠／树脂／金属归类、相对密度／回弹／摩擦、质量与转动惯量 |
| `ice-blue-mainline.js` | 十一模块顺序、1500–2500 目标、每胜一次券、状态清洗和安全商品链接 |
| `collection-game.js` | 收藏白名单、旧存档迁移、可见已拥有身份；保留旧玩法纯函数供兼容 |
| `collection-ui.js` | ready／playing／resolving／won／lost／closed 状态、目标 HUD、Canvas 预置件、DOM 投放件和揭晓生命周期 |
| `reward-3d-viewer.js` | 十一套程序几何、真实旋转／拖动、全幅不透明 WebGL／VSM、环境贴图与 GPU 资源释放 |
| `reward-studio.js` | 获批 V43 灰白连续纸面、程序纹理与光源；独立管理及释放自身资源 |
| `reward-framing.js` | 全幅背景下保持原模型屏幕大小与中心的相机换算，不改变模型几何 |
| `charm-geometry.js` | V42 首件封闭双面切割星星、梨形水滴、吊头／连接环及晶体材质；仅用于首件精修 |
| `tabletop-sound.js`、`game-sound.js`、`sample-sound.js` | 首次手势、静音、音乐／采样／揭晓混音、声部预算与取消 |
| `collection-catalog.js`、`tabletop-art.js` | 稳定配饰 ID、显示排除表、图册地址、独立素材加载与绘制 |
| `commerce.js` | HTTPS／无凭据外链规则；旧定制清单不自动发送 |

## 物理与时间

64 单位空间哈希生成局部碰撞候选，支撑感知、线性与角阻尼使密集物件回稳。接触图以相同身份判断，首次投放前预置组不自动清场；后续可消除组须包含玩家投放件。预置物由 Canvas 批量绘制，玩家投放物用 DOM 图层，二者读取同一实际物理状态。

游戏倒计时、连锁窗口按真实帧间隔扣减；消除帧也扣时间，截止优先于消除得分。V42 物理以 8ms 固定子步推进，每个渲染帧最多 4 步，超过 32ms 的物理时间计入 `droppedPhysicsMs`，不再用截断物理时间延长 60 秒倒计时。

四种材质已接入相对密度、回弹和摩擦；质量按尺寸与密度计算，碰撞法向冲量按逆质量分配，切向摩擦与转动惯量影响旋转。回稳由支撑、阻尼和睡眠条件控制。接触事件携带位置、材质对、法向速度、冲量与归一化强度，通过阈值和同对接触冷却去重；UI 将这些数据用于局部接触反馈和音效，不再用整盘抖动代替接触位置。

碰撞形状仍是圆形近似，不是 3D 揭晓网格或复合轮廓求解。[Node 测量](./evidence/material-physics-v42/report.json)中默认盘最大穿插 2.734 单位，平均速度、持续自转物件与末段接触音事件均为 0；两个高阶密集样本最大穿插仍为 12.730／6.814 单位，高于 4 单位门槛。该缺口不能由材质差异已接入或默认样本回稳替代验收。

## UI 与揭晓生命周期

运行仅允许一个活动回合。受管理的延时器绑定当前会话，关闭／重开取消旧回调，避免旧结算发奖、旧音效或旧焦点落到新页面。下一局同步刷新奖励 ID、目标图片和无障碍名称。

后台暂停 RAF、游戏时钟基线、揭晓计时器、3D viewer 与页面动画；回前台继续当前阶段，不销毁后留下静止空白。关闭则彻底结束场景。揭晓按钮在适当时点启用，减少动态模式提前可用；拖动角度不被自动帧覆盖。键盘可调整落点并投放，触控仍走同一游戏操作。

V44 由全幅不透明 WebGL viewer 渲染获批的灰白摄影棚，使用 VSM 阴影；`reward-framing.js` 根据原展示框换算相机，保持挂件的屏幕大小与中心。`collection-ui.js`／`collection.css` 配套更新背景、低对比图标及优惠券；旧蓝色光圈等装饰不再呈现在 won 状态。`reward-studio.js` 复用 V43 的纸面／光参数，只释放自身几何、材质、纹理和阴影资源，不销毁其他对象。

3D viewer 使用真实几何与 PBR 材质，失败时显示对应独立 2D 图，不阻断继续挑战。环境贴图、PMREM render target、几何／材质及 renderer 随实例清理；不能仅销毁生成器而遗留其 render target。保留的 refraction fill 是屏幕空间光学近似，不宣称物理真实折射或焦散；程序模型不等于参考照片扫描。

首件星星分支委托 `charm-geometry.js` 生成封闭前后切面、梨形水滴及连接五金，取代先前星体前方独立透明拼片；其余十个分支仍保留原实现。V44 没有修改模型、物理、声音、计分或优惠券逻辑；未实现尚待澄清的“ins风拆解”动画。V42 四视角和流程证据保留，V44 当前验证见 [TESTING](./TESTING.md)，不把几何封闭或背景批准等同于全部模型已达商品摄影精度。

## 音频

首次有效用户交互恢复 AudioContext，持久静音和后台状态参与每次播放判定。采样解码异步、失败合成降级，普通迟到点击超过 220ms 不补播。V41 揭晓采样不再短路原创主题旋律；常规合成预算 16 声部、揭晓 8 声部，二者总预算 24，采样与背景层另有自身生命周期。

环境音乐基础增益 0.35，揭晓期间压低后恢复。场景退出调用 `stopReveal`，同时取消已排队主题与采样，回到前台只恢复适用的底声，不补播已取消的整段开奖。声音不可用不阻断玩法；音频信号测试不证明设备实际扬声器效果。

V42 `impactOptions` 把求解器的材质、接触对、位置和强度传给声音层；`impactParameters` 控制采样及合成降级的音量、滤波、衰减和声像。微小静止接触不发声，不以每帧发生接触就重新触发采样。[离线信号报告](./evidence/material-physics-v42-audio/report.json)与真人听感分开记录。

## 存档与安全

`lucky-link.collection.v1` 当前 schema version 2，含合法珠序／暂存盒、原收藏、绳材、Note 与 `mainline`。主线仅接收十一模块白名单，券仅接收 5／10／15，读取时保留最近 100 条；过关次数独立保存。新键不存在时只读迁移旧 `lucky-link.styling.v2` 或 `lucky-link.studio.v1`，不写回旧键。

异常 unlocked／claimed 字段被忽略，不因损坏结构崩溃；保留合法可拥有珠序与已获主线记录。所有迁移出口统一过滤已禁展示配饰，避免流苏、粉骰子从旧草稿重新出现。没有为了测试修改 Owner 正常浏览器数据；测试使用隔离上下文。

收藏保存在当前浏览器；存储写入失败保留当次会话并提示 Session only，刷新可能丢失。不同源、设备、浏览器间不自动同步，不提供服务端防作弊或实物兑奖资格。

主线商品地址复用 `safeLink`：完整 HTTPS、无用户名／密码、无控制字符；非法地址回退批准的 example.com 占位。外链通过用户操作打开，不携带 Note。用户文字用文本插入，不当作 HTML。

## 保留兼容能力与验证边界

DIY 的串珠／盒子／撤销、绳材、Note／PNG 作品卡及历史 3D 仍保留，不是本轮重做目标。成品与导出读取同一实际珠序，图片无品牌或水印；系统保存／分享能力失败时保留用户作品。公开说明不把浏览器下载说成已保存到相册，也不虚构社区发布成功。

本轮未增加依赖或外部素材，未部署。Three.js 构建体积警告、素材总账缺口、移动网络性能、真实 iPhone 与设备音效验证仍按 [TESTING](./TESTING.md)／[RELEASE](./RELEASE.md) 披露。
